% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write.R
\name{archive_rows}
\alias{archive_rows}
\title{Archive (soft-delete) rows by setting an archived_at timestamp}
\usage{
archive_rows(.data, .reason = NULL, .by = NULL)
}
\arguments{
\item{.data}{A \code{feedr_tbl} from \code{\link[=get_table]{get_table()}}, optionally pre-filtered.}

\item{.reason}{Optional character string explaining why rows are archived.
Printed in the confirmation message for traceability.}

\item{.by}{Column name to use as the row key for the \code{WHERE} clause.
Defaults to auto-detecting the primary key.}
}
\value{
The original \code{feedr_tbl} invisibly (for piping).
}
\description{
Sets \code{archived_at = current_timestamp} on the rows identified by any
\code{\link[dplyr:filter]{dplyr::filter()}} applied before this call. Rows are never physically
deleted - they remain in the table but are logically inactive.
}
\details{
The table must have an \code{archived_at} column. Add one with:
\code{get_table("tbl") |> mutate_table(archived_at = as.POSIXct(NA))}
}
\examples{
\dontrun{
db <- init_feedr_db(seed = TRUE)

# Add the required column first
db |> get_table("ingredients") |> mutate_table(archived_at = as.POSIXct(NA))

# Archive rows matching a filter
db |> get_table("ingredients") |>
  dplyr::filter(active == FALSE) |>
  archive_rows(.reason = "retired ingredients")
}
}
