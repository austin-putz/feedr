% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/db.R
\name{close_feedr_db}
\alias{close_feedr_db}
\title{Close a feedr database connection}
\usage{
close_feedr_db(session)
}
\arguments{
\item{session}{A \code{feedr_session} object returned by \code{open_feedr_db()}.}
}
\value{
Invisibly returns \code{NULL}.
}
\description{
Explicitly disconnects the DuckDB connection held by a \code{feedr_session}.
The session is also closed automatically when garbage-collected, but calling
this function makes the intent clear and releases the file lock immediately.
}
\examples{
\dontrun{
session <- open_feedr_db()
close_feedr_db(session)
}
}
