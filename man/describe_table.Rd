% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/schema.R
\name{describe_table}
\alias{describe_table}
\title{Print column-level details for a single feedr table or view}
\usage{
describe_table(.con, .table_name = NULL)
}
\arguments{
\item{.con}{A \code{feedr_session} or \code{feedr_tbl}.}

\item{.table_name}{Table or view name as a single string.  Required when
\code{.con} is a \code{feedr_session}; ignored (with a message) when \code{.con} is a
\code{feedr_tbl} and \code{.table_name} matches the tbl's own table.}
}
\value{
A \code{data.frame} with columns \code{column_name}, \code{type}, \code{nullable},
\code{key}, and \code{description}, returned invisibly.
}
\description{
Displays each column's name, data type, nullability, key role (PK / FK),
and a one-line description.  Also prints the table description, row count,
and total column count as a header.
}
\details{
\code{.con} accepts either a \code{feedr_session} (pass \code{.table_name} explicitly) or
a \code{feedr_tbl} (\code{.table_name} is inferred automatically), so both of these
workflows work:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{db |> describe_table("nutrients")
db |> get_table("nutrients") |> describe_table()
}\if{html}{\out{</div>}}
}
\examples{
\dontrun{
db <- init_feedr_db()

# From a session
db |> describe_table("nutrients")

# From a feedr_tbl (table name inferred)
db |> get_table("nutrients") |> describe_table()

# Capture the result
cols <- db |> describe_table("ingredients")
cols$column_name
}
}
