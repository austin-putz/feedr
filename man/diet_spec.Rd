% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/requirements.R
\name{diet_spec}
\alias{diet_spec}
\title{Build a validated diet specification}
\usage{
diet_spec(
  .data,
  basis = NULL,
  source = NULL,
  species = NULL,
  production_class = NULL,
  spec_name = NULL,
  session = NULL,
  .save = TRUE
)
}
\arguments{
\item{.data}{A \code{feedr_tbl} (from \code{\link[=get_table]{get_table()}}) or a plain tibble/data
frame. Required columns: \code{nutrient_id}, at least one of
\code{requirement_min}/\code{requirement_max}/\code{requirement_target}, \code{unit_id},
\code{basis}. Optional columns that improve traceability: \code{feeding_phase_id},
\code{requirement_set_id}, \code{source}, \code{requirement_id}.}

\item{basis}{Character scalar; \code{"as_fed"} or \code{"dry_matter"}. When supplied,
every row must match this basis — no conversion is performed. Required when
rows within a phase have mixed \code{basis} values.}

\item{source}{Character scalar; provenance label written to
\code{diet_specs.source}, e.g. \code{"NRC2012"} or \code{"user_defined"}. Required when
rows within a phase have mixed \code{source} values or no \code{source} column.}

\item{species}{Character scalar; required for plain tibble input. Inferred
from \code{feeding_phases} when the input is a \code{feedr_tbl} with
\code{feeding_phase_id}.}

\item{production_class}{Character scalar; required for plain tibble input
when \code{production_class} is absent from the data.}

\item{spec_name}{Optional character scalar; a user label for the spec.
Defaults to \code{phase_name} from \code{feeding_phases}, then to
\code{production_class}. Must be unique among active (non-archived) specs when
supplied.}

\item{session}{A \code{feedr_session}; required when \code{.data} is a plain tibble
and \code{.save = TRUE}. Unnecessary when \code{.data} is a \code{feedr_tbl}.}

\item{.save}{Logical (default \code{TRUE}). When \code{TRUE}, write to \code{diet_specs}
and \code{diet_spec_nutrients} inside a transaction and return a \code{feedr_tbl}.
When \code{FALSE}, skip the write and return a plain preview tibble instead.}
}
\value{
When \code{.save = TRUE} (and a writable session is available): a
\code{feedr_tbl} pointing at the newly created \code{diet_specs} rows. Pass
directly to \code{formulate_diet()} or collect with \code{\link[dplyr:collect]{dplyr::collect()}}.

When \code{.save = FALSE} or no session is available: a plain collected tibble
of the normalized nutrient rows (preview only — cannot be passed to
\code{formulate_diet()}).
}
\description{
Accepts a filtered nutrient requirements table (from the database or a
manually constructed tibble), validates all requirement values and units,
normalizes values to solver-canonical LP units, and saves the result to
\code{diet_specs} and \code{diet_spec_nutrients}. Returns a \code{feedr_tbl} pointing at
the newly created \code{diet_specs} rows.
}
\details{
When \code{feeding_phase_id} is present in the input, the table is automatically
split by phase. Each phase produces one row in \code{diet_specs} and N rows in
\code{diet_spec_nutrients}. All phases are validated before any write occurs.

\strong{No S3 object is created.} The database tables are the sole
representation of the spec, so any downstream edits are immediately visible
to \code{formulate_diet()}.
}
\examples{
\dontrun{
feedr <- init_feedr_db("~/feedr/swine.db")

# Standard use: filter requirements, call diet_spec(), pipe to formulate_diet()
spec_tbl <- feedr |>
  get_table("nutrient_requirements") |>
  dplyr::filter(requirement_set_id == "standard_swine_2025",
                is.na(archived_at)) |>
  diet_spec(basis = "as_fed", source = "NRC2012")

# Preview without saving
preview <- feedr |>
  get_table("nutrient_requirements") |>
  dplyr::filter(requirement_set_id == "standard_swine_2025") |>
  diet_spec(basis = "as_fed", .save = FALSE)

# Manual tibble with explicit session
manual_spec <- tibble::tribble(
  ~nutrient_id, ~requirement_min, ~requirement_max, ~unit_id,  ~basis,
  "ne_swine",   2450,             NA,               "kcal_kg", "as_fed",
  "sid_lys",    0.95,             NA,               "pct",     "as_fed"
) |>
  diet_spec(species = "swine", production_class = "grower",
            basis = "as_fed", source = "user_defined", session = feedr)
}
}
