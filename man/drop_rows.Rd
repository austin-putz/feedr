% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write.R
\name{drop_rows}
\alias{drop_rows}
\title{Permanently delete rows from a feedr table}
\usage{
drop_rows(.data, .by = NULL, .all = FALSE, .allow_computed = FALSE)
}
\arguments{
\item{.data}{A \code{feedr_tbl} from \code{\link[=get_table]{get_table()}}, optionally pre-filtered.}

\item{.by}{Column name to use as the row key for the \code{WHERE} clause.
Defaults to auto-detecting the primary key. Ignored when \code{.all = TRUE}.}

\item{.all}{If \code{FALSE} (default), delete only the filtered rows. If
\code{TRUE}, delete every row in the table regardless of any prior filter.}
}
\value{
The original \code{feedr_tbl} invisibly (for piping).
}
\description{
Physically removes rows from the table. \strong{This cannot be undone.}
For a reversible alternative, see \code{\link[=archive_rows]{archive_rows()}}.
}
\details{
Two modes:
\itemize{
\item \strong{Filtered} (\code{.all = FALSE}, default) - deletes only the rows matched
by any prior \code{\link[dplyr:filter]{dplyr::filter()}} on the \code{feedr_tbl}.
\item \strong{Wipe} (\code{.all = TRUE}) - deletes every row in the table. A WARNING is
printed before any data is removed.
}
}
\examples{
\dontrun{
db <- init_feedr_db()

# Delete one row matched by a filter
db |> get_table("ingredients") |>
  dplyr::filter(ingredient_id == "salt") |>
  drop_rows()

# Wipe all rows from a table
db |> get_table("units") |> drop_rows(.all = TRUE)
}
}
\seealso{
\code{\link[=archive_rows]{archive_rows()}} for a soft-delete that keeps data recoverable.
}
