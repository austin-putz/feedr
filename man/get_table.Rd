% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tables.R
\name{get_table}
\alias{get_table}
\alias{filter.feedr_tbl}
\alias{select.feedr_tbl}
\alias{collect.feedr_tbl}
\title{Open a database table as a lazy, pipe-friendly object}
\usage{
get_table(feedr, name)

\method{filter}{feedr_tbl}(.data, ...)

\method{select}{feedr_tbl}(.data, ...)

\method{collect}{feedr_tbl}(x, ...)
}
\arguments{
\item{feedr}{A \code{feedr_session} created by \code{\link[=init_feedr_db]{init_feedr_db()}}.}

\item{name}{Table name as a single character string, e.g. \code{"ingredients"}.}

\item{.data}{A \code{feedr_tbl}.}

\item{...}{Filtering expressions passed to \code{\link[dplyr:filter]{dplyr::filter()}}, column
selections passed to \code{\link[dplyr:select]{dplyr::select()}}, or additional arguments passed
to \code{\link[dplyr:collect]{dplyr::collect()}}.}

\item{x}{A \code{feedr_tbl}.}
}
\value{
A \code{feedr_tbl} object. Use \code{\link[dplyr:filter]{dplyr::filter()}} to narrow rows,
\code{\link[dplyr:collect]{dplyr::collect()}} to pull into a tibble, or pipe directly into a write
function.
}
\description{
Returns a \code{feedr_tbl} containing a lazy DuckDB table ready for filtering,
collecting, or passing to write functions like \code{\link[=append_rows]{append_rows()}},
\code{\link[=archive_rows]{archive_rows()}}, \code{\link[=update_rows]{update_rows()}}, and \code{\link[=mutate_table]{mutate_table()}}.
}
\examples{
\dontrun{
db <- init_feedr_db(seed = TRUE)

# View a table
db |> get_table("ingredients")

# Filter then collect
db |> get_table("ingredients") |>
  dplyr::filter(ingredient_class == "grain") |>
  dplyr::collect()
}
}
