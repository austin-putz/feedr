% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/db.R
\name{init_feedr_db}
\alias{init_feedr_db}
\title{Initialize a feedr database session}
\usage{
init_feedr_db(path = NULL, migrate = FALSE, read_only = FALSE)
}
\arguments{
\item{path}{Full path to the database file, or \code{":memory:"} for an
in-memory session with no file on disk. When \code{NULL}, the path is resolved
from options: \code{getOption("feedr.db_path")} (directory, default
\code{":memory:"}) joined with \code{getOption("feedr.db_name")} (filename, default
\code{"feedr.db"}). Any file extension is accepted — common choices are
\code{feedr.db}, \code{feedr.duckdb}, or a project-specific name like \code{swine.db}.}

\item{migrate}{If \code{TRUE}, run pending schema migrations on an existing
database (e.g. add tables introduced in a newer schema version). Has no
effect on new databases or on read-only connections.}

\item{read_only}{Open in read-only mode. Cannot be used when the file does
not yet exist.}
}
\value{
A \code{feedr_session} object containing \code{con} (DBI connection), \code{path},
\code{read_only}, \code{schema_version}, and \code{opened_at}. The DuckDB connection
closes automatically when the session object is garbage-collected.
}
\description{
Creates or opens a DuckDB database and returns a \code{feedr_session} object.
}
\details{
Two primary uses:
\itemize{
\item \strong{New database} — creates the file and builds the initial schema.  Use a
future \code{seed_data()} function to populate it with reference values.
\item \strong{Existing database} — opens the file as-is, prints the exact path with
OS-specific instructions to delete it if you want to start over. The file
is never overwritten through this function.
}
}
