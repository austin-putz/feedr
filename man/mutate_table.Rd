% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write.R
\name{mutate_table}
\alias{mutate_table}
\title{Add one or more columns to a feedr table}
\usage{
mutate_table(.data, ..., .default = TRUE)
}
\arguments{
\item{.data}{A \code{feedr_tbl} from \code{\link[=get_table]{get_table()}}. Any prior \code{\link[dplyr:filter]{dplyr::filter()}}
is ignored - \verb{ALTER TABLE} applies to the entire table.}

\item{...}{Named arguments of the form \code{col_name = value}. The value
sets the column type and optionally the default:
\itemize{
\item \code{"swine"} or \code{NA_character_} -> \code{VARCHAR}
\item \code{1L} or \code{NA_integer_} -> \code{INTEGER}
\item \code{5.0} or \code{NA_real_} -> \code{DOUBLE}
\item \code{TRUE} / \code{FALSE} / bare \code{NA} -> \code{BOOLEAN}
\item \code{as.Date("2026-01-01")} / \code{as.Date(NA)} -> \code{DATE}
\item \code{as.POSIXct(NA)} / \code{Sys.time()} -> \code{TIMESTAMP}
}}

\item{.default}{\code{TRUE} (default) - use the provided value as the SQL
\code{DEFAULT} for all new columns. \code{FALSE} - add columns with no default
(existing rows get \code{NULL}). Supply a logical vector of the same length
as \code{...} to control each column independently.}
}
\value{
The original \code{feedr_tbl} invisibly (for piping).
}
\description{
Runs \verb{ALTER TABLE ... ADD COLUMN} for each column specified in \code{...}.
The R value you provide determines the column type \strong{and}, when
\code{.default = TRUE}, becomes the SQL \code{DEFAULT} for that column.
}
\examples{
\dontrun{
db <- init_feedr_db()

# VARCHAR DEFAULT 'swine'
db |> get_table("ingredients") |> mutate_table(species = "swine")

# DATE DEFAULT '2026-01-01'
db |> get_table("ingredients") |>
  mutate_table(effective_date = as.Date("2026-01-01"))

# Two columns: first gets default, second does not
db |> get_table("ingredients") |>
  mutate_table(region = "central_iowa", count = 0L, .default = c(TRUE, FALSE))
}
}
