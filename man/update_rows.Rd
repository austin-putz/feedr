% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write.R
\name{update_rows}
\alias{update_rows}
\title{Update column values in existing rows}
\usage{
update_rows(.data, ..., .rows = NULL, .by = NULL)
}
\arguments{
\item{.data}{A \code{feedr_tbl} from \code{\link[=get_table]{get_table()}}, optionally pre-filtered.}

\item{...}{Named scalar column-value pairs, e.g. \code{active = FALSE}.
Vectors are rejected. Cannot be combined with \code{.rows}.}

\item{.rows}{A data frame of updated values. Must include the \code{.by} column.
Cannot be combined with \code{...}.}

\item{.by}{Key column name used to match rows when \code{.rows} is supplied.
Defaults to auto-detecting the primary key.}
}
\value{
The original \code{feedr_tbl} invisibly (for piping).
}
\description{
Two modes:
\itemize{
\item \strong{Scalar} - pass \code{col = value} and the same value is applied to every
row that passed any prior \code{\link[dplyr:filter]{dplyr::filter()}}. Vectors are not accepted.
\item \strong{Tibble} - pass \code{.rows} (a data frame) and \code{.by} (the key column) to
update each row individually.
}
}
\examples{
\dontrun{
db <- init_feedr_db()

# Scalar mode - set active = FALSE for all mineral ingredients
db |> get_table("ingredients") |>
  dplyr::filter(ingredient_class == "mineral") |>
  update_rows(active = FALSE)

# Tibble mode - update specific rows by primary key
db |> get_table("ingredients") |>
  update_rows(
    .rows = tibble::tibble(
      ingredient_id = c("limestone", "salt"),
      active        = c(TRUE, FALSE)
    ),
    .by = "ingredient_id"
  )
}
}
